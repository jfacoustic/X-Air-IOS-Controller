# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'X-Air Controls' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for X-Air Controls
  pod 'SwiftOSC', '~> 1.2'

  target 'X-Air ControlsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'X-Air ControlsUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
